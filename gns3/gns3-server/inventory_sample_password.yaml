server:
  hosts:
    "IP address of GNS3 server":
  vars:
    # ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_user: gns3
    ansible_password : gns3
    asible_become_pass: gns3
    gns3_password: gns3
